name: MLP Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch: 

jobs:
  ping_render:
    runs-on: ubuntu-latest
    steps:
      - name: Send Heartbeat to Render
        run: |
          curl -s -o /dev/null -w "%{http_code}" ${{ secrets.MLP_URL }}
